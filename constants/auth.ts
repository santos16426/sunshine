const AUTH_TOKEN_KEY = 'auth_token'
const SESSION_DURATION_MS = 10 * 60 * 1000 // 10 minutes

const WARNING_THRESHOLD_MS = 1 * 60 * 1000; // show warning 2 minutes before expiry
const TICK_INTERVAL_MS = 1_000;
export { AUTH_TOKEN_KEY, SESSION_DURATION_MS, WARNING_THRESHOLD_MS, TICK_INTERVAL_MS }